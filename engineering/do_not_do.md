❌ Never use lorem ipsum

❌ Never use stock photos

❌ Never create generic AI sections

❌ Never add random gradients

❌ Never use more than two button styles

❌ Never hardcode spacing

❌ Never hardcode colors

❌ Never use heavy shadows

❌ Never add animations just because they look cool

❌ Never sacrifice performance for aesthetics

❌ Never introduce a dependency without justification

❌ Never break existing components to solve a local problem
